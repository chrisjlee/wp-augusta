jQuery(document).ready( function() {
	// var slideshowimg = jQuery('#branding-contents img');
	// if(jQuery(slideshowimg).length >1 ) {
		// jQuery('#branding-contents').height(jQuery(slideshowimg).height()).innerfade({
			// animationtype:  'fade', 	//Type of animation 'fade' or 'slide' (Default: 'fade'), 
			// speed: '30000', 				//Fadingspeed in milliseconds or keywords (slow, normal or fast)(Default: 'normal'), 
			// timeout: 	'10000',			//Time between the fades in milliseconds (Default: '2000'), 
			// type:  	'sequence',			//Type of slideshow: 'sequence', 'random' or 'random_start' (Default: 'sequence'), 
			// containerheight: '153',		//Height of the containing element in any css-height-value (Default: 'auto')  
			// //runningclass: 			//CSS-Class which the container get�s applied (Default: 'innerfade') 
		// });
	// }
});